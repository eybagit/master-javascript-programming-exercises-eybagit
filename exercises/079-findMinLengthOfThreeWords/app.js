const findMinLengthOfThreeWords = (palabra1, palabra2, palabra3) => Math.min(palabra1.length, palabra2.length, palabra3.length,);

console.log(findMinLengthOfThreeWords('Esta es la mas larga', 'Cortica', 'Intermedia'));
